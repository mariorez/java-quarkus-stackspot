type: app
description: Vanilla Quarkus with "Hello World" endpoint
# template used to create the project
template: java-quarkus-stackspot/vanilla-template
inputs:
  some_input: value
# list of plugins to be applied after project creation
plugins:
  - plugin: java-quarkus-stackspot/<plugin-name>
    inputs:
      some_input: value
      some_list:
        - item1
        - item2
